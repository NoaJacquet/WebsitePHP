CREATE TABLE QUESTION(
   idQ INT PRIMARY KEY NOT NULL,
   typeQ TEXT NOT NULL,
   labelQ TEXT NOT NULL,
   correctAnswerQ CHAR(50) NOT NULL
);

CREATE TABLE REPONSE(
   answer TEXT NOT NULL,
   idQ INT NOT NULL,
   PRIMARY KEY (answer, idQ),
   FOREIGN KEY (idQ) REFERENCES QUESTION (idQ)
);